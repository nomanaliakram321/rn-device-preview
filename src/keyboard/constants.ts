/**
 * Height (in simulated-device logical pixels) of the fake on-screen keyboard.
 * Roughly matches a real iOS/Android keyboard's height at a mid-size phone
 * width — good enough for layout-testing purposes, not pixel-accurate.
 */
export const SIMULATED_KEYBOARD_HEIGHT = 291;
